# สรุปโปรเจค Agent Hierarchy API (ภาษาไทย)

## ภาพรวมโปรเจค

สร้าง Web API ด้วย .NET 6 พร้อม Entity Framework Core สำหรับจัดการระบบลำดับชั้นตัวแทน (Agent Hierarchy)

## ✅ สิ่งที่สร้างเสร็จแล้ว

### 1. ออกแบบฐานข้อมูล ✓

**3 ระดับตำแหน่ง (Ranks):**
- **AG** (Agent General) - ระดับพื้นฐาน (Level 1)
- **AL** (Agent Leader) - ระดับกลาง (Level 2)
- **AE** (Agent Executive) - ระดับสูงสุด (Level 3)

**9 ระดับลำดับชั้น (Hierarchies):**
- **A1, A2, A3** → สำหรับ AG
- **A4, A5, A6** → สำหรับ AL
- **A7, A8, A9** → สำหรับ AE

**โครงสร้างลำดับชั้น:**
```
AE (ผู้บริหาร)
  ↓
AL (หัวหน้าทีม)
  ↓
AG (ตัวแทนทั่วไป)
```

### 2. โครงสร้างตัวแทน (Agent Structure)

แต่ละตัวแทนมี:
- **AgentCode** - รหัสตัวแทน (เช่น AG0001, AL0001, AE0001)
- **AgentName** - ชื่อตัวแทน
- **Hierarchy** - ระดับลำดับชั้น (A1-A9)
- **Rank** - ตำแหน่ง (AG, AL, AE)
- **ParentAgent** - ตัวแทนหัวหน้า (สามารถมีหรือไม่มีก็ได้)

### 3. ตัวอย่างข้อมูลที่สร้างไว้แล้ว

```
AE0001 (A9, AE) - ผู้บริหาร 1
├── AL0001 (A6, AL) - หัวหน้าทีม 1
│   ├── AG0001 (A3, AG) - ตัวแทนทั่วไป 1
│   └── AG0002 (A2, AG) - ตัวแทนทั่วไป 2
└── AL0002 (A5, AL) - หัวหน้าทีม 2
    └── AG0003 (A1, AG) - ตัวแทนทั่วไป 3
```

## 🚀 วิธีใช้งาน

### ขั้นตอนที่ 1: ตั้งค่าฐานข้อมูล

แก้ไขไฟล์ `appsettings.json`:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=localhost;Database=AgentHierarchyDB;Trusted_Connection=True;TrustServerCertificate=True;"
  }
}
```

**Connection String สำหรับ SQL Server แต่ละแบบ:**

- **LocalDB**: `Server=(localdb)\\mssqllocaldb;Database=AgentHierarchyDB;Trusted_Connection=True;TrustServerCertificate=True;`
- **SQL Express**: `Server=localhost\\SQLEXPRESS;Database=AgentHierarchyDB;Trusted_Connection=True;TrustServerCertificate=True;`
- **SQL Server ปกติ**: `Server=localhost;Database=AgentHierarchyDB;User Id=sa;Password=YourPassword;TrustServerCertificate=True;`

### ขั้นตอนที่ 2: รันโปรแกรม

```bash
cd AgentHierarchyApi
dotnet run
```

### ขั้นตอนที่ 3: เข้าใช้งาน API

- **Swagger UI**: https://localhost:5001
- **API Base URL**: https://localhost:5001/api

ฐานข้อมูลจะถูกสร้างและใส่ข้อมูลตัวอย่างอัตโนมัติเมื่อรันครั้งแรก

## 📋 API Endpoints (จุดเชื่อมต่อ API)

### จัดการตัวแทน (Agents)

| Method | URL | คำอธิบาย |
|--------|-----|----------|
| GET | `/api/agents` | ดูตัวแทนทั้งหมด |
| GET | `/api/agents/{id}` | ดูตัวแทนตาม ID |
| GET | `/api/agents/code/{agentCode}` | ดูตัวแทนตามรหัส (เช่น AG0001) |
| GET | `/api/agents/rank/{rankCode}` | ดูตัวแทนตามตำแหน่ง (AG, AL, AE) |
| GET | `/api/agents/hierarchy/{hierarchyCode}` | ดูตัวแทนตามลำดับชั้น (A1-A9) |
| GET | `/api/agents/hierarchy-tree` | ดูโครงสร้างลำดับชั้นทั้งหมด |
| POST | `/api/agents` | สร้างตัวแทนใหม่ |
| PUT | `/api/agents/{id}` | แก้ไขข้อมูลตัวแทน |
| DELETE | `/api/agents/{id}` | ลบตัวแทน (ไม่ได้ลบจริง แค่ทำเครื่องหมายว่าไม่ใช้งาน) |

### จัดการตำแหน่ง (Ranks)

| Method | URL | คำอธิบาย |
|--------|-----|----------|
| GET | `/api/ranks` | ดูตำแหน่งทั้งหมด |
| GET | `/api/ranks/{rankCode}` | ดูตำแหน่งตามรหัส |

### จัดการลำดับชั้น (Hierarchies)

| Method | URL | คำอธิบาย |
|--------|-----|----------|
| GET | `/api/hierarchies` | ดูลำดับชั้นทั้งหมด |
| GET | `/api/hierarchies/{hierarchyCode}` | ดูลำดับชั้นตามรหัส |
| GET | `/api/hierarchies/rank/{rankCode}` | ดูลำดับชั้นตามตำแหน่ง |

## 💡 ตัวอย่างการใช้งาน

### สร้างตัวแทนใหม่

```bash
curl -X POST https://localhost:5001/api/agents \
  -H "Content-Type: application/json" \
  -d '{
    "agentCode": "AG0010",
    "agentName": "ตัวแทนทดสอบ",
    "hierarchyCode": "A1",
    "parentAgentId": 2
  }'
```

### ดูโครงสร้างลำดับชั้นทั้งหมด

```bash
curl https://localhost:5001/api/agents/hierarchy-tree
```

### ดูตัวแทนตามตำแหน่ง

```bash
# ดูตัวแทนระดับ AG ทั้งหมด
curl https://localhost:5001/api/agents/rank/AG

# ดูตัวแทนระดับ AL ทั้งหมด
curl https://localhost:5001/api/agents/rank/AL

# ดูตัวแทนระดับ AE ทั้งหมด
curl https://localhost:5001/api/agents/rank/AE
```

### ดูตัวแทนตามลำดับชั้น

```bash
# ดูตัวแทนในระดับ A1
curl https://localhost:5001/api/agents/hierarchy/A1
```

## 🔒 กฎการตรวจสอบ (Validation Rules)

ระบบมีการตรวจสอบดังนี้:

1. **รหัสตัวแทนต้องไม่ซ้ำ**: แต่ละตัวแทนต้องมีรหัสไม่ซ้ำกัน
2. **ลำดับชั้นถูกต้อง**: 
   - AG (level 1) ต้องอยู่ใต้ AL (level 2)
   - AL (level 2) ต้องอยู่ใต้ AE (level 3)
   - AE (level 3) สามารถอยู่บนสุด หรืออยู่ใต้ AE อื่นได้
3. **ความสัมพันธ์หัวหน้า-ลูกน้อง**: ระดับต่ำไม่สามารถเป็นหัวหน้าระดับสูงได้
4. **การลบข้อมูล**: การลบจะไม่ลบข้อมูลจริง แต่จะทำเครื่องหมายว่าไม่ใช้งาน

## 📊 ตารางในฐานข้อมูล

### ตาราง Ranks (ตำแหน่ง)
- Id - รหัสอ้างอิง
- RankCode - รหัสตำแหน่ง (AG, AL, AE)
- RankName - ชื่อตำแหน่ง
- Level - ระดับ (1, 2, 3)

### ตาราง Hierarchies (ลำดับชั้น)
- Id - รหัสอ้างอิง
- HierarchyCode - รหัสลำดับชั้น (A1-A9)
- HierarchyName - ชื่อลำดับชั้น
- RankId - อ้างอิงไปยังตำแหน่ง
- Level - ระดับ (1-9)

### ตาราง Agents (ตัวแทน)
- Id - รหัสอ้างอิง
- AgentCode - รหัสตัวแทน (เช่น AG0001)
- AgentName - ชื่อตัวแทน
- HierarchyId - อ้างอิงไปยังลำดับชั้น
- ParentAgentId - อ้างอิงไปยังหัวหน้า (อาจเป็น null ได้)
- RankId - อ้างอิงไปยังตำแหน่ง
- IsActive - สถานะการใช้งาน
- CreatedDate - วันที่สร้าง
- UpdatedDate - วันที่แก้ไขล่าสุด

## 🛠️ เทคโนโลยีที่ใช้

- **Framework**: .NET 6.0
- **Database**: SQL Server
- **ORM**: Entity Framework Core 6.0
- **API Documentation**: Swagger/OpenAPI
- **Architecture**: Repository Pattern + Service Layer

## ✨ ความสามารถของระบบ

✅ เพิ่ม/แก้ไข/ลบ/ดูข้อมูลตัวแทน (CRUD)
✅ รองรับโครงสร้างแบบลำดับชั้น (Parent-Child)
✅ ตรวจสอบความถูกต้องของลำดับชั้นอัตโนมัติ
✅ ค้นหาตัวแทนตามตำแหน่งหรือลำดับชั้น
✅ แสดงโครงสร้างลำดับชั้นแบบต้นไม้
✅ การลบแบบ Soft Delete (ไม่ลบข้อมูลจริง)
✅ มีเอกสาร API แบบ Interactive (Swagger UI)
✅ ใส่ข้อมูลตัวอย่างอัตโนมัติ

## 📁 ไฟล์เอกสาร

- **README.md** - คู่มือการใช้งานแบบเต็ม (ภาษาอังกฤษ)
- **QUICKSTART.md** - คู่มือเริ่มต้นใช้งานอย่างรวดเร็ว
- **DatabaseDesign.md** - เอกสารออกแบบฐานข้อมูลแบบละเอียด
- **PROJECT_SUMMARY.md** - สรุปโปรเจคแบบละเอียด
- **สรุปโปรเจค_TH.md** - เอกสารฉบับนี้ (ภาษาไทย)

## 🎯 สถานะโปรเจค

✅ **ออกแบบฐานข้อมูล**: เสร็จสมบูรณ์
✅ **สร้าง Web API**: เสร็จสมบูรณ์  
✅ **รองรับ 3 ตำแหน่ง**: AG, AL, AE
✅ **รองรับ 9 ระดับ**: A1-A9
✅ **โครงสร้างลำดับชั้น**: AE → AL → AG
✅ **ระบบจัดการตัวแทน**: พร้อมใช้งาน
✅ **Build สำเร็จ**: 0 ข้อผิดพลาด, 0 คำเตือน

## 🚦 วิธีทดสอบระบบ

### 1. เปิด Swagger UI
เปิดเบราว์เซอร์ไปที่ https://localhost:5001

### 2. ทดสอบดูข้อมูล
- คลิกที่ `GET /api/agents` แล้วกด "Try it out" และ "Execute"
- ระบบจะแสดงตัวแทนทั้งหมด

### 3. ทดสอบดูโครงสร้างลำดับชั้น
- คลิกที่ `GET /api/agents/hierarchy-tree`
- ระบบจะแสดงโครงสร้างแบบต้นไม้

### 4. ทดสอบสร้างตัวแทนใหม่
- คลิกที่ `POST /api/agents`
- ใส่ข้อมูล JSON และกด Execute

## 🎓 สิ่งที่ได้เรียนรู้จากโปรเจคนี้

1. การออกแบบฐานข้อมูลแบบลำดับชั้น (Hierarchical Database Design)
2. การใช้ Entity Framework Core กับ SQL Server
3. Repository Pattern และ Service Layer
4. RESTful API Design
5. Swagger/OpenAPI Documentation
6. การตรวจสอบข้อมูล (Validation)
7. การจัดการ Relationships ใน EF Core

## 💪 พร้อมใช้งาน

โปรเจคนี้พร้อมใช้งานแล้ว สามารถนำไปปรับแต่งหรือพัฒนาต่อได้ตามความต้องการ

---

**สถานะ**: ✅ เสร็จสมบูรณ์และพร้อมใช้งาน
**Framework**: .NET 6.0
**ฐานข้อมูล**: SQL Server with Entity Framework Core
